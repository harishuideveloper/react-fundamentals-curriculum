var React = require('react');
var ReactDOM = require('react-dom');
var App = require('./components/App')
require('./index.css');

// Things to consider while creating a component
// State
// Lifecycle Events
// UI

ReactDOM.render(
    <App />,
    document.getElementById('app')
)